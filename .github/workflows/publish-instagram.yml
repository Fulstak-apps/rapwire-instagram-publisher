name: RapWire 24/7 monitor and publisher

on:
  push:
    branches: [main]
    paths-ignore:
      - "queue/**"
      - "media/**"
  workflow_dispatch:
  schedule:
    - cron: "7,27,47 * * * *"

permissions:
  contents: write

concurrency:
  group: rapwire-instagram-publisher
  cancel-in-progress: false

env:
  NARRO_RSS_URL: "https://rss.narro.info/e4f36406-0664-4e77-b672-7e0682966a9f"
  MAX_NEW_ITEMS: "6"
  MAX_SOURCE_AGE_HOURS: "24"

jobs:
  monitor-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install AI publisher dependencies
        run: python -m pip install --disable-pip-version-check pillow openai

      - name: Pull every fresh Narro story and let AI curate RapWire-worthy news
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python scripts/ai-curate-narro.py

      - name: Remove stale ready stories
        run: python scripts/expire-stale-queue.py

      - name: Commit AI-curated queue and original comic graphics
        run: |
          if [ -z "$(git status --porcelain -- queue media)" ]; then
            echo "No queue/media changes."
            exit 0
          fi
          git config user.name "RapWire 24/7"
          git config user.email "actions@users.noreply.github.com"
          git add queue media
          git commit -m "AI-curate Narro stories and generate RapWire comic art"
          git pull --rebase origin main
          git push

      - name: Prepare publishable JPEG media
        run: python scripts/convert-media-to-jpeg.py

      - name: Commit converted JPEG media
        run: |
          if [ -z "$(git status --porcelain -- queue media)" ]; then
            echo "No converted media changes."
            exit 0
          fi
          git config user.name "RapWire 24/7"
          git config user.email "actions@users.noreply.github.com"
          git add queue media
          git commit -m "Prepare RapWire publishable JPEG media"
          git pull --rebase origin main
          git push

      - name: Wait for public media propagation
        run: sleep 10

      - uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Publish next ready RapWire story
        env:
          RAPWIRE_BRAND: "24/7"
          INSTAGRAM_ACCESS_TOKEN: ${{ secrets.INSTAGRAM_ACCESS_TOKEN }}
          INSTAGRAM_USER_ID: ${{ secrets.INSTAGRAM_USER_ID }}
          THREADS_ACCESS_TOKEN: ${{ secrets.THREADS_ACCESS_TOKEN }}
          THREADS_USER_ID: ${{ secrets.THREADS_USER_ID }}
          PUBLISH_INSTAGRAM_STORIES: "true"
          MAX_FEED_POSTS_PER_RUN: "1"
        run: node scripts/publish-instagram.mjs

      - name: Save publication log
        if: ${{ always() }}
        run: |
          if [ -z "$(git status --porcelain -- queue)" ]; then exit 0; fi
          git config user.name "RapWire 24/7"
          git config user.email "actions@users.noreply.github.com"
          git add -- queue
          git commit -m "Log RapWire publication" || true
          git pull --rebase origin main || true
          git push || true
